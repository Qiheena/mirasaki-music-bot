services:
  client:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    volumes:
      - ./config.js:/app/config.js
      - ./RasaVedic-music-bot.db:/app/RasaVedic-music-bot.db